# claude-artifacts プロジェクトのルール

このリポジトリは、学習テーマごとの調査結果をHTMLレポートにまとめ、GitHub上でWebサイトとして公開するためのもの。

## 構成
- reports/ … 生成したHTMLレポートを置く（1テーマ1ファイル）
- index.html … レポート一覧のトップページ。新しいレポートを追加したら必ずリンクを足す
- reports/usage-log.csv … 各レポート生成にかかったトークン数・推定コストの記録。フックが自動で追記するので手で編集しない
- usage-dashboard.html … 上記CSVをグラフと表で見るためのページ

## ルール
- 出力・報告はすべて日本語
- ファイル名は `reports/<英小文字とハイフンのスラッグ>.html`。日付は付けない。同名があれば末尾に -2, -3 を付ける
- HTMLは1ファイル完結（CSS・図はインライン）。外部画像は使わない
- コミット対象は reports/ 配下の新規ファイルと index.html のみ（usage-log.csv は専用フックが別コミットで管理する）
- `git push --force` は絶対に使わない。push に失敗したら作業を止めて報告する
- 出典サイトの文章を長く丸写ししない。自分の言葉で要約し、参考リンクを付ける

## コスト記録の仕組み
`.claude/hooks/log-usage.js` が git push の直後に自動実行され、直前の /report 命令以降で
使ったトークン数を会話ログから集計してAPI料金換算の目安を出し、reports/usage-log.csv に追記して自分で push する。
この処理はメインの作業とは独立しているので、失敗しても /report の結果には影響しない。
料金表は log-usage.js 内の PRICING 定数で管理しており、実際の料金改定があれば更新が必要。
